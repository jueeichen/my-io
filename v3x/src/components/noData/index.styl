@import '../../static/css/common.styl';

.no-data-content {
  width: 750rpx;
  position: fixed;

  _flex-center-column();

  >text {
    font-size: 28rpx;
    font-weight: 400;
    color: #999999;
    line-height: 34rpx;
  }
}
